const state={balance:0,threshold:0};
const els={balance:document.getElementById('currentBalance'),threshold:document.getElementById('safetyThreshold'),endMonth:document.getElementById('endMonthBalance'),endYear:document.getElementById('endYearBalance')};
let chart;
function euro(v){return new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR'}).format(v)}
function calc(){const now=new Date();const endMonth=new Date(now.getFullYear(),now.getMonth()+1,0);const endYear=new Date(now.getFullYear(),11,31);const months=endYear.getMonth()-now.getMonth()+1;const recurring=0;const endMonthBal=state.balance-recurring;const endYearBal=state.balance-(recurring*months);els.endMonth.textContent=euro(endMonthBal);els.endYear.textContent=euro(endYearBal);draw(now,endYearBal)}
function draw(start,val){const labels=[];const data=[];const threshold=[];let saldo=state.balance;for(let m=start.getMonth();m<=11;m++){labels.push(new Date(2025,m,1).toLocaleString('it-IT',{month:'short'}));saldo-=0;data.push(saldo);threshold.push(state.threshold)}if(chart)chart.destroy();chart=new Chart(document.getElementById('balanceChart'),{type:'line',data:{labels,datasets:[{label:'Saldo stimato',data,borderColor:'#2457ff',tension:.3},{label:'Soglia minima',data:threshold,borderColor:'#d93025',borderDash:[6,6]}]}})}
els.balance.oninput=e=>{state.balance=Number(e.target.value);calc()};els.threshold.oninput=e=>{state.threshold=Number(e.target.value);calc()};